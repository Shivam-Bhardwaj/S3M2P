[package]
name = "mcad-core"
version = "0.1.0"
edition = "2021"

[dependencies]
dna = { path = "../../DNA" }
glam.workspace = true
wgpu.workspace = true  # GPU compute - only external dep for hardware access
# Everything else written from scratch in Rust
