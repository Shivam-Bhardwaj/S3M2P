[package]
name = "ecad-core"
version = "0.1.0"
edition = "2021"

[dependencies]
dna = { path = "../../DNA" }
serde.workspace = true
# Write own Gerber X2 generator from scratch - no external ECAD deps
